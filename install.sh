# Symlinks

ln -s $HOME/dotfiles/.vim $HOME/.vim || echo "$HOME/.vim already exist"
ln -s $HOME/dotfiles/.zshrc $HOME/.zshrc || echo "$HOME/.zshrc already exist"
ln -s $HOME/dotfiles/.bashrc $HOME/.bashrc || echo "$HOME/.bashrc already exist"
ln -s $HOME/dotfiles/.aliasrc $HOME/.aliasrc || echo "$HOME/.aliasrc already exist"
ln -s $HOME/dotfiles/.gitconfig $HOME/.gitconfig || echo "$HOME/.gitconfig already exist"
ln -s $HOME/dotfiles/.config/git/ignore $HOME/.config/git/ignore || echo "$HOME/.config/git/ignore already exist"
